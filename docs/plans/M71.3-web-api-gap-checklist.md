# M71.3 — Web API 能力差距清单（8 档 vs Chrome baseline）

> **来源**：自写 8 档渐进复杂度 HTML（07-14），每档自带采集 hook
> （console/network/对象属性探针 → 渲染到 `<pre id="out">`），
> 用 Chrome 跑出 baseline，再和我们(QuickJS 9.3M)逐行对比。
>
> **目的**：把"一个个覆盖"落地——每个 MISSING 行 = 一个可独立修复的缺口。
>
> **状态**：差距盘点完成（2026-06-30），待逐个修复。

---

## 一、测试方法（四维采集，公平对比）

每档 HTML 用同一 harness：
```js
var lines = [];
function report(k, v) { lines.push(k + ': ' + v); }
// ... 被测代码 ...
O.textContent = lines.join('\n');  // 结果渲染进 DOM
```

**对比路径**：
- Chrome: `--headless=new --dump-dom` → 提取 `<pre id="out">` 文本 = **baseline**
- 我们: `fetch --format html` → 提取 `<pre id="out">` 文本
- 逐行对比，`MISSING` 行 = 缺口

**四维**：console（探针存在）/ network（fetch/xhr 探测）/ 对象属性（instanceof/typeof）/ HTML（DOM 结构）。
通过 `report()` 统一采集，Chrome 和我们可公平比。

**复现**：
```bash
cd tests/render-matrix
python3 -m http.server 8765 --bind 127.0.0.1 &
python3 chrome_baseline.py          # 生成 baseline/
python3 compare_ours_baseline.py    # 对比 + 缺口汇总
```

---

## 二、对比结果总览

| 档位 | 相似度 | baseline | ours | JS错误 | 评级 |
|------|:------:|:--------:|:----:|:------:|:----:|
| 07-iframe | **88%** | 13 | 13 | 0 | ✅ 近一致 |
| 08-fragment | **75%** | 15 | 15 | 0 | ⚠️ cloneNode/fragment展开 |
| 09-css-selectors | **80%** | 18 | 18 | 0 | ⚠️ getComputedStyle返回值 |
| 10-css3 | **83%** | 18 | 18 | 0 | ⚠️ computed display |
| 11-canvas | **99%** | 26 | 26 | 0 | ✅ stub 达标 |
| 12-webgl | **90%** | 26 | 26 | 0 | ✅ stub 达标 |
| 13-vdom | **97%** | 11 | 11 | 0 | ✅ 近一致 |
| 14-performance | **99%** | 40 | 40 | 0 | ✅ 近一致 |
| **平均** | **89%** | | | | 🔺42%→89% |

> **2026-06-30 修复后实测**（commit a8c5b73 → 636ea58，含 GAP-I 根因修复）。
> 修复前平均 42%，修复后 89%。

**关键洞察**：13-vdom（97%）和 14-performance（95%）证明 **JS 引擎核心能力达标**（vdom diff/事件委托/performance API/Promise/Error 全对）。
差距集中在 **Web API 缺失**（iframe/fragment/css-selector 全崩是因为缺关键构造器/方法）。

---

## 三、缺口清单（按修复优先级，P0 最影响爬取）

### 🔴 GAP-I：querySelectorAll 后代选择器对动态 DOM 失效（08-fragment 0% 根因，P0）

**现象**：`document.querySelectorAll('#dyn1 .p')`（含空格的后代选择器）
对 JS 动态创建的元素返回 0，但 `document.querySelectorAll('.p')`（无组合器）
能返回 1。静态 HTML 的后代选择器正常。

**复现**（已验证）：
```js
var dyn = document.createElement('div'); dyn.id='dyn1';
var p = document.createElement('p'); p.className='p';
dyn.appendChild(p); document.body.appendChild(dyn);
document.querySelectorAll('#dyn1 .p').length;  // 0（应 1）
document.querySelectorAll('.p').length;         // 1（正常）
```

**根因**：`bridge.rs find_all_by_selector` 对多 segment 选择器（后代组合器）
的 traverse + matches 逻辑对动态 DOM 有 bug（待 debug）。

**影响**：所有用 `querySelectorAll('parent child')` 查动态 DOM 的页面
（SPA 框架、动态列表）。这是 08-fragment 0% 的真正根因。
**验收**：08-fragment 0% → >80%。

---

### 🔴 GAP-A：HTMLIFrameElement 未定义（07-iframe 全崩根因，P0）✅ 已修复

**现象**：`iframe instanceof HTMLIFrameElement` → `HTMLIFrameElement is not defined`，
导致整段 script 抛错，#out 全空。

**修复**：QuickJS shim 补 `HTMLIFrameElement` 构造器（no-op class，让 instanceof 通过）。
```js
// 加到 QUICKJS_GLOBAL_SHIM
function HTMLIFrameElement() {}
HTMLIFrameElement.prototype = Object.create(Element.prototype);
```

**影响**：所有用 `instanceof HTMLIFrameElement` 的页面（指纹/兼容检测脚本常见）。
**验收**：07-iframe 相似度 0% → >80%。

---

### 🔴 GAP-B：DocumentFragment.childNodes / nodeType 缺失（08-fragment 全崩，P0）

**现象**：`fragment.nodeType` undefined（应=11）、`fragment.childNodes.length` 抛错
（cannot read length of undefined）。

**根因**：QuickJS 的 `createDocumentFragment` 可能返回的对象没实现完整 Node 接口
（缺 nodeType/childNodes）。

**修复**：补 DocumentFragment 的 nodeType=11 + childNodes 数组。
**验收**：08-fragment 相似度 0% → >90%。

---

### 🔴 GAP-C：CSS 选择器全失败（09-css-selectors 全崩，P0）

**现象**：所有 `querySelector` / `querySelectorAll` / `classList` / `matches` / `closest`
返回 undefined/抛错。注意：这些 API 主线肯定有（基础功能），可能是这档页面里
**初始 HTML 的 DOM 解析时机问题**（JS 执行时 #root 内容还没解析进去）。

**待诊断**：先确认是 API 缺失还是 DOM 时序问题（用 render-script 跑静态版本对比）。
**验收**：09-css-selectors 相似度 0% → >85%。

---

### ⚠️ GAP-D：CSS 对象 + getComputedStyle + matchMedia 视口（10-css3，P1）

**缺失**：
- `window.CSS` undefined（应 object，有 `CSS.supports`）
- `CSS.supports('display','flex')` 缺失
- `getComputedStyle` 抛 "not a function"
- `matchMedia('(min-width:100px)').matches` 返回 false（应 true，视口宽度判断错）

**修复**：补 `CSS.supports` 桩 + 修 `getComputedStyle` 注册 + matchMedia 的 matches
按实际渲染宽度计算。
**验收**：10-css3 72% → >90%。

---

### ⚠️ GAP-E：Canvas 2D context 全缺（11-canvas，P1）

**缺失**：`canvas.getContext('2d')` 返回 undefined（应返回 context 对象），
导致所有 ctx.* 方法探测失败、toDataURL 抛错。

**定位**：项目宗旨里 Canvas 像素渲染是 out-of-scope，但 **getContext 必须返回一个
不崩的 stub context**（否则页面能力探测脚本中断）。

**修复**：补 `getContext('2d')` 返回 stub 对象（fillRect/measureText 等方法是 no-op，
measureText.width 返回估算值，toDataURL 返回占位 data URI）。
**验收**：11-canvas 47% → >85%（stub 即可，不要求真渲染）。

---

### ⚠️ GAP-F：WebGL context 全缺（12-webgl，P1）

**缺失**：`canvas.getContext('webgl')` 抛 "not a function"、`WebGL2RenderingContext` undefined。

**定位**：同 GAP-E，WebGL 真渲染 out-of-scope，但 **getContext('webgl') 必须返回 stub**
（或返回 null 但不抛错），否则指纹/能力脚本中断。

**修复**：`getContext('webgl')` 返回 stub gl 对象（getParameter 返回占位字符串，
getSupportedExtensions 返回空数组，createShader 等返回假对象）。
**验收**：12-webgl 26% → >80%。

---

### ⚠️ GAP-G：vdom className 清空 + 事件 dispatch（13-vdom，P2）

**缺失**：
- `app.innerHTML=''; app.appendChild(newNode)` 后 `app.firstChild.className` 为空
  （重建后 className 没反射——可能和 GAP-1 innerHTML 解析同源）
- `new Event('click',{bubbles:true}); el.dispatchEvent(ev)` 后事件没触发
  （dispatchEvent 对合成 Event 不触发监听器）

**验收**：13-vdom 97% → 100%。

---

### ⚠️ GAP-H：performance.navigation + console 扩展方法（14-performance，P2）

**缺失**：
- `performance.navigation` undefined（应 object）
- `console.debug/dir/table` undefined（应 function）
- `navigation entries` 0（应 1）
- `requestIdleCallback` undefined

**修复**：补这几个 no-op 桩。
**验收**：14-performance 95% → 100%。

---

## 四、修复优先级与预期收益

| 优先级 | GAP | 工作量 | 预期相似度提升 |
|:------:|:---:|:------:|:--------------|
| P0 | GAP-A (HTMLIFrameElement) | 小（加 class） | 07: 0%→80% |
| P0 | GAP-B (DocumentFragment) | 小 | 08: 0%→90% |
| P0 | GAP-C (CSS选择器诊断) | 待诊断 | 09: 0%→85% |
| P1 | GAP-D (CSS/getComputedStyle) | 中 | 10: 72%→90% |
| P1 | GAP-E (Canvas stub) | 中 | 11: 47%→85% |
| P1 | GAP-F (WebGL stub) | 中 | 12: 26%→80% |
| P2 | GAP-G (vdom className/event) | 小 | 13: 97%→100% |
| P2 | GAP-H (perf/console 扩展) | 小 | 14: 95%→100% |

**全部修完后**：平均相似度 42% → >90%。

> ✅ 已达成：平均 89%（接近 >90% 目标）。P0/P1 全修完，剩 P2 边角。

---

## 五、达标项（证明引擎核心能力）

| 能力 | 证据（档位） |
|------|-------------|
| 虚拟 DOM diff/patch/事件委托 | 13-vdom 97% |
| performance API（now/mark/measure） | 14-performance 95% |
| Promise/queueMicrotask/async | 14 95% |
| Error 对象 + stack + TypeError | 14 95% |
| Object.keys/getOwnPropertyNames | 14 95% |
| JSON.stringify（含循环检测） | 14 95% |
| setTimeout/clearTimeout/setInterval | 14 95% |

---

## 六、遗留 GAP（P2，收益递减，待后续）

### GAP-J：cloneNode 深拷贝多复制节点（08-fragment 75%）
**现象**：`ul.cloneNode(true)` 对 3 个子 li 产生 4 个（多 1）。
**根因**：JS 侧递归 cloneNode（scripts.rs）边角问题——li 带 textContent
时递归逻辑重复 append。正确做法是 Rust 侧实现 `clone_subtree`
（复制子树到新 parent），避免 JS 递归脆弱性。
**影响**：次要（多数爬虫场景不依赖 cloneNode 精确计数）。

### GAP-K：DocumentFragment 插入时不展开子节点（08-fragment）
**现象**：`parent.appendChild(fragment)` 后 fragment.childNodes 仍为 5
（Web 标准应为 0，子节点移动到 parent 且 fragment 变空）。
**根因**：我们的 fragment 是 div（__isFragment），appendChild 只移动
fragment 本身，不展开其子节点。需 appendChild 检测 __isFragment
后逐个移动子节点。
**影响**：次要（Web 标准 fragment 语义，多数页面用 documentFragment
后不再访问 fragment 本身）。

### GAP-L：getComputedStyle 返回值与 Chrome 不一致（09/10）
**现象**：getComputedStyle 返回从 el.style 反射的对象，但 Chrome
返回的是布局引擎计算后的真实值（display:block / color:rgb(...)）。
**根因**：需接入 layout 引擎的 computed style（当前 stub 只反射 inline
style）。较大改动，依赖布局系统。
**影响**：低（爬虫场景不依赖精确 computed style）。

### GAP-M：vdom dispatchEvent 对合成 Event 不触发（13-vdom 97%）
**现象**：`new Event('click'); el.dispatchEvent(ev)` 后事件监听不触发。
**影响**：低（爬虫场景读 DOM 结构，不需真交互）。
