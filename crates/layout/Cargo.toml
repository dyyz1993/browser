[package]
name = "browser-layout"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Block / inline / flex layout engine"

[dependencies]
browser-dom = { path = "../dom" }
browser-css-engine = { path = "../css-engine" }

[dev-dependencies]
